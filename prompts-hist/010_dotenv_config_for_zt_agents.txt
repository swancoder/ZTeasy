2026-05-22 — .env support for zt-agents via spring-dotenv

Task: Remove the manual `export ANTHROPIC_API_KEY=...` step from the zt-agents setup flow.

Changes made:
- Added `.env.example` to repo root documenting all configurable zt-agents keys.
- Added `spring-dotenv 4.0.0` to version catalog and zt-agents build.gradle.kts.
- Updated README prerequisites section: `cp .env.example .env` replaces the export instruction.
- ADR-008 documents the dependency choice, precedence rules, and gitignore risk.

Key prompt pattern: treat config ergonomics as a first-class concern — if a developer needs to
remember a shell command before running a service, that's a setup friction bug worth fixing.
