Task: create ZTeasy/docs/SPEC.md (if such file doesn't exist) with full
technical specification and implementation roadmap flagging the progress.

Context: no SPEC.md existed. Consolidate what's spread across README.md
(quick start, chain-of-trust summary), CLAUDE.md (terse per-stage changelog),
and the nine ADRs (individual decisions, each with alternatives/CoT/critique)
into one technical reference: architecture, component specs, data model, API
reference, config reference, zero-trust model, roadmap with progress flags
(done vs backlog), and a consolidated risk register pulled from each ADR's
self-critique section.
